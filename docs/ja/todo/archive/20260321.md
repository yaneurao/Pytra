# 2026-03-21 完了タスク

## P0-CPP-GENERATED-RUNTIME-PIPELINE-01: .east → C++ ヘッダー生成パイプライン

- `runtime_generation_manifest.json` に C++ built_in/std ターゲットを追加。
- `.east` → `.h` 生成パイプラインを整備し、`src/runtime/cpp/generated/` の `#include` を修正。

## P0-UNION-ARG-ESCAPE-01/02/03: union 型引数への escape 判定

- union type 引数を持つクラスを gc_managed (ref) に強制するパーサー暫定検出を実装。
- `Path` が gc_managed として emit され `object` に格納可能になったことを検証。
- escape 解析結果を `class_storage_hint` に反映する仕組みを実装。

## P0-ESCAPE-TO-STORAGE-HINT-01: escape 解析結果の class_storage_hint 反映

- `optimize_linked_program` で union type パラメータに含まれるクラスの `class_storage_hint` を `"ref"` に昇格する処理を実装 (S1)。
- `core_module_parser.py` の暫定判定を除去 (S2)。
- `Path` がリンカー段で gc_managed になることを検証済み (S3)。

## P0-LEGACY-API-CLEANUP-01: 旧 object API + 旧互換モード一掃

- emitter の `make_object` 生成箇所を `object(...)` に置換 (S1)。
- emitter の `obj_to_rc_or_raise<T>` 生成箇所を `object::as<T>()` に置換 (S2)。
- runtime の `make_object` / `obj_to_rc_or_raise` 互換 shim を削除 (S3)。
- C++ list の value/pyobj モード互換 (`cpp_list_model`) を削除し ref モードに統一 (S4)。
- pathlib repro の g++ ビルド検証済み (S5: P0-5 link 統合で解決)。

## P0-RUNTIME-EAST-IN-LINK-PIPELINE-01: runtime .east を link パイプラインに統合

- `resolved_dependencies_v1` に含まれる runtime モジュールの .east を LinkedProgram に自動追加 (S1)。
- `write_cpp_rendered_program` で runtime モジュールも link emit の出力に含める (S2)。
- `_generate_runtime_east_headers`（standalone transpile）を廃止 (S3)。
- 旧 object API を新 object API（`unbox`/`as`/`is`/`_cpp_str_lit`）に統一 (S4)。
- pathlib repro の g++ ビルド検証済み (S5)。

## P0-OBJECT-IS-TAGGED-VALUE-01: object = tagged value 統一

- `object` 定義を `{pytra_type_id tag; rc<RcObject> _rc;}` に変更し暗黙変換コンストラクタ・`unbox`/`as`/`is` を追加 (S1)。
- emitter が union 型に `object` を emit し `_Union_*` typedef を廃止 (S2)。
- emitter の cast/isinstance/暗黙代入を `object::unbox`/`object::as`/`object::is` に変更 (S3)。
- pathlib.py を含む `out/cpp/` g++ ビルドを検証済み (S4)。
- 互換レイヤを除去し新 API のみに移行 (S5)。

## P0-PY-RUNTIME-H-DECOMPOSITION-01: py_runtime.h 分解

- `core/str_methods.h` 分離 (S1)。
- `core/conversions.h` 分離 (S2)。
- `built_in/dict_ops.h` 分離 (S3)。
- `built_in/bounds.h` 分離 (S4)。
- `core/type_id_support.h` 分離・循環依存解消 (S5)。
- `core/rc_ops.h` 分離 (S6)。
- `py_runtime.h` を include のみのファサードに書き換え (S7)。
- emitter が個別ヘッダーを emit するよう変更（multi-file モードは pytra_multi_prelude.h 経由）(S8)。

## P0-SELF-CONTAINED-CPP-OUTPUT-01: out/cpp/ 自己完結ビルドディレクトリ

- `py_runtime.h` と native ヘッダーの include パスを namespace 基準の相対パスに変更 (S1)。
- `write_cpp_rendered_program` を拡張し native runtime を `out/cpp/{namespace}/` にコピー (S2)。
- emitter の `#include` 出力パスを `out/cpp/` 基準に変更 (S3)。
- Makefile 生成を `out/cpp/` 自己完結に対応 (S4)。
- `pytra-cli.py --build` フローを新パイプラインに対応 (S5)。
- 最小 repro が `out/cpp/` 内で `make` でビルドできることを検証済み (S6)。

## P0-TAGGED-UNION-OBJECT-BOX-01: tagged union を object + type_id に統一

- tagged union 宣言を `using X = PyTaggedValue;` に変更し `PyBoxed`/`py_box`/`py_unbox`/`PyTaggedValue` を runtime に追加 (S1)。
- emitter の cast を変更（POD は `py_unbox<T, TID>(v.value)`、クラスは `static_cast`）(S2)。
- isinstance narrow 後の暗黙代入で unbox を emit。inline union のマッチング修正 (S3)。
- `PyTaggedValue` に暗黙変換コンストラクタ追加 (S4)。
- pathlib.py を含む `out/cpp/` g++ ビルドを検証済み (S5)。
- 他バックエンドへの展開方針を確認 (S6)。

## P0-LINKER-RESOLVED-INCLUDES-01: リンカーによる C++ include パス確定

- `global_optimizer.py` に `_build_resolved_dependencies` を実装 (S1)。
- `module.py` の `_collect_import_cpp_includes` を修正 (S2)。
- `runtime_symbol_index.json` の `compiler_headers` を実体パスに修正 (S3)。
- `from pytra.std.pathlib import Path` の最小 repro が g++ でビルドできることを検証済み (S4)。
- `src/runtime/cpp/generated/` と manifest の C++ ターゲットを撤去 (S5)。

## P1-PIPELINE-STAGE-SEPARATION-01: パイプライン段分離 — compile / link / emit の独立化

- `east2cpp.py` を新設し、linked EAST JSON → C++ multi-file emit を実装 (S1)。
- `py2x.py --link-only` 出力と `east2cpp.py` 入力の接続 (S2)。
- `pytra-cli.py --build` を east2cpp.py サブプロセス呼び出しに変更 (S3)。
- `build_selfhost.py --multi-module` を 3 段パイプライン（compile → link → emit）に分離 (S4)。

## P4-VARARG-EAST3-LOWERING-01: `*args` vararg サポート — EAST3 レベル脱糖

- `east2_to_east3_lowering.py` に post-pass を追加: FunctionDef の `vararg_name/type` を `list[T]` 通常引数に変換し、モジュール内 Call サイトをパック (S1)。
- `global_optimizer.py` に `_apply_vararg_callsite_packing_global` を追加: クロスモジュール Call サイトをパック (S2)。
- `pytra/std/pathlib.py` に `joinpath(*parts: str | Path) -> Path` を追加 (S3)。

## P0-GPP-COMPILE-HEADERS-01: g++ コンパイルテストが built_in/string_ops.h を見つけられない問題修正

- `tools/gen_runtime_cpp_from_east.py` で 26 件の `.east` → `.h` + `.cpp` を生成。
- `build_multi_cpp.py` + テストに `-Isrc/runtime/generated` を追加。

## P1-BACKEND-REGISTRY-DECOUPLING-01: backend_registry 依存の除去

- `pytra-cli.py` の C++ emit パスを `east2cpp.py` サブプロセスに変更し、`backend_registry` import を除去 (S1)。
- 非 C++ emit パスを `east2x.py` サブプロセスに変更 (S2)。
- `pytra-cli.py` から `backend_registry_static` import を除去 (S3)。
- selfhost compile+link が 151 モジュールから 65 モジュールに削減（非 C++ backend 74 件を除去、57% 削減）(S4)。

## P1-EMIT-DIRECT-CALL-01/02: 各言語 emit ラッパーの個別 emitter 直接呼び出し化

- 14 言語のラッパーを個別 emitter 直接呼び出しに変更。各ラッパーは自分の emitter のみ import (01)。
- `toolchain/emit/all.py` を削除 (02)。

## P1-CPP-STATICMETHOD-EMIT-01: C++ emitter @staticmethod 対応

- `emit_function` で `@staticmethod`/`@classmethod` デコレータを検出し、`static` を emit するよう修正。

## P1-CPP-PATH-RC-TYPE-01: C++ emitter で Path が rc<Path> でなく bare 型で宣言される問題修正

- `pathlib.east` の `class_storage_hint` を `"value"` から `"ref"` に修正。

## P7-SELFHOST-NATIVE-COMPILER-ELIM-01: native/compiler/ 完全削除

- selfhost ビルドパイプラインを EAST3 JSON 入力専用に統一し、`transpile_cli.cpp` の `.py` シェルアウトパスを除去 (S1)。
- `emit_source_typed` のシェルアウトを除去。生成 C++ emitter (transpile_to_cpp) の直接呼び出しに置換 (S2)。
- `src/runtime/cpp/compiler/` を削除。シェルアウトゼロ確認済み。契約インベントリから削除済みファイル参照を除去 (S3)。

## P7-SELFHOST-MULTIMOD-TRANSPILE-01: selfhost multi-module transpile 基盤構築

- emitter モジュール群の selfhost 制約準拠を監査（ブロッカー: 動的 dispatch 4 件）(S1)。
- `pytra.std.pathlib.Path` に `relative_to` / `with_suffix` を実装 (S1a)。
- `pytra.std.re` に `compile` / `Pattern` を実装 (S1b)。
- `multifile_writer.py` の `import os` を `pytra.std` 経由に移行 (S1c)。
- CppEmitter の動的 mixin 注入 (`_attach_cpp_emitter_helper_methods` の `setattr`/`__dict__`) を EAST3 mixin 展開による多重継承に置換。`install_py2cpp_runtime_symbols` の `globals()` 注入を除去 (S1d)。
- `tools/build_selfhost.py` を multi-module transpile パイプライン（compile → link）に拡張。全 150 モジュール EAST3 コンパイル成功 (S2)。
- selfhost 66 モジュールの C++ emit 完了。233 C++ ファイル (4.1MB) を生成 (S3)。
- リンカーの wildcard re-export 伝播を実装。151 モジュールの link に成功 (S4)。
