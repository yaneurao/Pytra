# 2026-03-27 TODO アーカイブ

P1-CLOSURE-DEF 全完了、P2-LINK-INPUT-COMPLETENESS 全完了、P1-ISINSTANCE-NARROWING 全完了、P1-ISINSTANCE-POD 全完了分をアーカイブ。

---

## P1-CLOSURE-DEF: nested FunctionDef の ClosureDef lowering — 全完了

文脈: [docs/ja/plans/p1-closure-def-lowering.md](../../plans/p1-closure-def-lowering.md)

1. [x] [ID: P1-CLOSURE-DEF-S1] EAST3 の ClosureDef ノード仕様を spec-east.md に追加
2. [x] [ID: P1-CLOSURE-DEF-S2] EAST3 lowering でキャプチャ解析 + ClosureDef 生成を実装
3. [x] [ID: P1-CLOSURE-DEF-S3] fixture 追加（nested function のキャプチャパターン）+ golden 生成
4. [x] [ID: P1-CLOSURE-DEF-S4] 各 emitter の ClosureDef 写像実装 + parity 確認

## P2-LINK-INPUT-COMPLETENESS: link 層の入力完全性検証 — 全完了

文脈: [docs/ja/plans/p2-link-input-completeness.md](../../plans/p2-link-input-completeness.md)

1. [x] [ID: P2-LINK-COMPLETE-S1] link 層で import 解決の完全性検証を実装（未解決 import を fail-closed で報告）— `link_modules()` が未提供 module_id を列挙して停止
2. [x] [ID: P2-LINK-COMPLETE-S2] runtime / stdlib モジュールのホワイトリスト（検証除外対象）を定義 — `pytra.*` と runtime importer 由来の外部 stdlib import を除外
3. [x] [ID: P2-LINK-COMPLETE-S3] 型スタブ生成の仕組みを設計・実装（parse 不能モジュール向け）— `toolchain2.link.type_stubgen` を追加し、欠落 8 module の EAST3 stub 生成と link 成功回帰を実装
4. [x] [ID: P2-LINK-COMPLETE-S4] selfhost 37本に対して完全性検証を実行し、欠落モジュールを確認 — current source 再生成ベースの回帰テストを追加

備考: selfhost の Go build 失敗の根本原因が link-input の不完全性（欠落 8 module）であることを特定。stored golden ではなく current source 再生成ベースで評価すべきことも判明。stub で link まで通過済み。

## P1-ISINSTANCE-NARROWING: isinstance 後の自動型ナローイング — 全完了

文脈: [docs/ja/plans/p1-isinstance-narrowing.md](../../plans/p1-isinstance-narrowing.md)

1. [x] [ID: P1-NARROW-S1] resolve に isinstance 条件式の検出 + if/elif ブロック内型環境更新を実装
2. [x] [ID: P1-NARROW-S2] early return guard の対応（`if not isinstance(x, T): return` 後の fallthrough narrowing）
3. [x] [ID: P1-NARROW-S3] ternary isinstance の対応（`y = x if isinstance(x, T) else None`）
4. [x] [ID: P1-NARROW-S4] 再代入検出による narrowing 無効化の実装
5. [x] [ID: P1-NARROW-S5] fixture 追加（全 narrowing パターン）+ golden 生成 + parity 確認

備考: resolver/EAST に narrowing を集約し、emitter での guard-aware scope 追跡は不採用。`JsonVal` は `dict[str,JsonVal]` / `list[JsonVal]` へ narrow。`and` 連結条件と `not isinstance(...) or ...: continue` guard も v1 に含めた。

## P1-ISINSTANCE-POD: POD 型の isinstance exact match 実装 + golden 生成 — 全完了

文脈: [docs/ja/plans/p1-isinstance-pod-exact.md](../../plans/p1-isinstance-pod-exact.md)

1. [x] [ID: P1-ISINSTANCE-POD-S1] パーサー / resolve が `isinstance(x, int16)` 等の POD 型判定を処理できるようにする
2. [x] [ID: P1-ISINSTANCE-POD-S2] EAST3 で POD isinstance を exact match 命令に lower する
3. [x] [ID: P1-ISINSTANCE-POD-S3] `isinstance_pod_exact.py` の golden 生成（east1/east2/east3/east3-opt/linked）
4. [x] [ID: P1-ISINSTANCE-POD-S4] C++ / Go emitter で compile + run + stdout 一致（`py_assert_stdout` 通過）

備考: fixture/golden は既に配置済みだったため、2026-03-27 時点で active TODO と実装状態の不整合を解消した。検証は `test_spec_conform.py` / `test_linker_spec_conform2.py` の POD isinstance 回帰に加え、`isinstance_pod_exact.py` を C++ で `pytra-cli.py build --target cpp --run`、Go で `pytra-cli2.py -build --target go` 後にローカル Go toolchain で `go build && ./app.out` 実行して `True` を確認した。

## P1-PARSER-STDLIB-JSON: pytra.std.json の parser 対応 + golden 再生成 — 全完了

文脈: [docs/ja/plans/p1-parser-stdlib-json.md](../../plans/p1-parser-stdlib-json.md)

1. [x] [ID: P1-PARSER-JSON-S1] parser で `type X = ...`（PEP 695 type alias）構文を処理できるようにする
2. [x] [ID: P1-PARSER-JSON-S2] Union 内の再帰的 forward reference を解決できるようにする
3. [x] [ID: P1-PARSER-JSON-S3] `pytra.std.json.py` が parse → resolve → compile → optimize → link を通過することを確認
4. [x] [ID: P1-PARSER-JSON-S4] golden 再生成 + 既存 parity 維持確認
