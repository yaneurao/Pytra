<a href="../../../ja/todo/archive/20260401.md">
  <img alt="日本語で読む" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

# 2026-04-01 Completed Tasks

## Java Backend

### P0-JAVA-TYPE-ID-CLEANUP: Removing type_id_table References from Java Runtime

Spec: [docs/ja/spec/spec-adt.md](../../spec/spec-adt.md) §6

Java has native `instanceof`, so references to the `pytra_built_in_type_id_table` class are unnecessary.

1. [x] [ID: P0-JAVA-TYPEID-CLN-S1] Removed `type_id_table` class references from `src/runtime/java/built_in/PyRuntime.java` (2026-04-01) — retired reflective lookup for `pytra_built_in_type_id_table`; built-in type IDs are now wired directly to runtime-internal constants
2. [x] [ID: P0-JAVA-TYPEID-CLN-S2] Replaced Java emitter isinstance with `x instanceof Type` (2026-04-01) — verified that `_render_isinstance_check` in the existing emitter implementation [java_native_emitter.py](/workspace/Pytra/src/toolchain/emit/java/emitter/java_native_emitter.py) uses only `instanceof`
3. [x] [ID: P0-JAVA-TYPEID-CLN-S3] Verified no regression in fixture + sample + stdlib parity (2026-04-01) — `PYTHONPATH=src:tools/check python3 tools/check/runtime_parity_check_fast.py --targets java`, `--case-root sample`, `--case-root stdlib` give fixture 137/137, sample 18/18, stdlib 16/16 PASS respectively

### P0-JAVA-NEW-FIXTURES: Java parity for New Fixtures

Confirming Java parity for fixtures added this session. All pass in Python.

1. [x] [ID: P0-JAVA-NEWFIX-S1] Verified `tuple_unpack_variants` achieves compile + run parity PASS in Java (2026-04-01) — fixed old EAST3 lowering missed nested `FunctionDef -> ClosureDef` / non-Name listcomp target assignment; corrected Java emitter ClosureDef helper emit, unpack, try/catch, dynamic Eq, `print(bool)`; confirmed `True` with `pytra-cli.py --target java --run`
2. [x] [ID: P0-JAVA-NEWFIX-S2] Verified `typed_container_access` achieves compile + run parity PASS in Java (2026-04-01) — added `dict.items/keys/values` lowering, `tuple_expanded` runtime-iter unpack, container assignment cast correction, and tuple type Java conversion to Java emitter; confirmed `True` with `pytra-cli.py --target java --run`
3. [x] [ID: P0-JAVA-NEWFIX-S3] Verified `in_membership_iterable` achieves compile + run parity PASS in Java (2026-04-01) — moved Java emitter `In/NotIn` to `PyRuntime.pyIn` base, lowered `range` to `PyRuntime.pyRange`, lowered `set[T]` to `LinkedHashSet<T>`; confirmed `True` with `pytra-cli.py --target java --run`
4. [x] [ID: P0-JAVA-NEWFIX-S4] Verified `callable_higher_order` achieves compile + run parity PASS in Java (2026-04-01) — added minimal higher-order support treating `callable` as `Function<Object,Object>` to Java emitter, lowered function values to cast-wrapped lambdas, converted callable calls to `.apply(...)`; confirmed `True` with `pytra-cli.py --target java --run`
5. [x] [ID: P0-JAVA-NEWFIX-S5] Verified `object_container_access` achieves compile + run parity PASS in Java (2026-04-01) — removed general-union rejection in old Java emitter to allow object lane, added `set()` builtin and `LinkedHashSet` coercion; confirmed `True` with `pytra-cli.py --target java --run`

### P0-JAVA-LINT-V2: Resolving Remaining Java emitter Hardcode Lint Issues

`check_emitter_hardcode_lint.py --lang java` shows `skip_pure_python` FAIL.

1. [x] [ID: P0-JAVA-LINT-V2-S1] Fixed skip_pure_python violation — removed pure Python modules from `skip_modules` in mapping.json (2026-04-01) — removed `pytra.std.` from `src/runtime/java/mapping.json`
2. [x] [ID: P0-JAVA-LINT-V2-S2] Verified 0 violations in all categories with `check_emitter_hardcode_lint.py --lang java` (2026-04-01) — 0 violations

## C# Backend

### P0-CS-TYPE-ID-CLEANUP: Removing PYTRA_TID_* / pytra_isinstance from C# Runtime

Spec: [docs/ja/spec/spec-adt.md](../../spec/spec-adt.md) §6

C# has native `is` / `switch`, so the 26 `PYTRA_TID_*` constants, `pytra_isinstance`, and `type_id_table` are unnecessary.

1. [x] [ID: P0-CS-TYPEID-CLN-S1] Removed `PYTRA_TID_*` constants and `pytra_isinstance` from `src/runtime/cs/built_in/py_runtime.cs` — replaced with neutral internal type constants and deleted `pytra_isinstance`. Added `py_is_set` and Python-style container/tuple repr to the runtime to maintain C# parity.
2. [x] [ID: P0-CS-TYPEID-CLN-S2] Deleted `src/runtime/cs/generated/built_in/type_id.cs` — deleted the generated `type_id.cs`. `check_emitter_hardcode_lint.py --lang cs --include-runtime` shows 0 violations.
3. [x] [ID: P0-CS-TYPEID-CLN-S3] Replaced C# emitter isinstance with `x is Type t` — migrated `isinstance` checks for builtins/user classes/containers in the toolchain2 C# emitter to native `is` checks. Also normalized legacy `PYTRA_TID_*` expected types to type names.
4. [x] [ID: P0-CS-TYPEID-CLN-S4] Verified no regression in fixture + sample + stdlib parity — fixture `137/137 PASS`, sample `18/18 PASS`, stdlib `16/16 PASS`.
