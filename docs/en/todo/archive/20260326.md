<a href="../../../ja/todo/archive/20260326.md">
  <img alt="日本語で読む" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

# 2026-03-26 TODO Archive

P1-EMIT-GO-PARITY fully completed; P1-SPEC-CONFORM2 phases 2 and 3 completed items archived.

---

## P1-EMIT-GO-PARITY: Go emitter compile + run parity Fixes — Fully Completed

Context: [docs/ja/plans/p1-emit-go-parity.md](../../plans/p1-emit-go-parity.md)

1. [x] [ID: P1-GO-PARITY-S1] Implemented Go translation of list/dict comprehensions
2. [x] [ID: P1-GO-PARITY-S2] Fixed missing EAST: lowered `write_text` etc. to BuiltinCall via resolve, added with-statement parser support, and propagated bytearray types
3. [x] [ID: P1-GO-PARITY-S3] `go run` + stdout match across 132 fixtures
4. [x] [ID: P1-GO-PARITY-S4] `go run` + stdout match across 18 samples (including PNG/GIF artifact CRC32 matches)

## P1-SPEC-CONFORM2: Specification Conformance Phase 2+3 — Fully Completed

Context: [docs/ja/plans/p1-spec-conform2.md](../../plans/p1-spec-conform2.md)

**Phase 2: emit (removing workarounds)**

7. [x] [ID: P1-SPEC-CONFORM2-S7] `emit/go/emitter.py`: Removed type inference, type mutation, added casts, and hardcoded module detection — deleted `Name` fallback in `_emit_unbox()`, synced the runtime lane to toolchain2 output, and maintained `json_extended` Go parity
8. [x] [ID: P1-SPEC-CONFORM2-S8] `emit/cpp/emitter.py`: Same — removed target/value type inference in `Assign` and the `unknown -> int64` fallback in `VarDecl`; `unknown` is now `auto`, range targets use EAST3 `target_type`, and skipped runtime values are handled via metadata + mapping
9. [x] [ID: P1-SPEC-CONFORM2-S9] `emit/common/code_emitter.py`: Consolidated to branch solely on mapping.json — gathered skipped runtime symbol/value name resolution and import binding → runtime helper name resolution into common helpers, and removed duplicated `mapping.calls` direct references and prefix construction in Go/C++ emitters

**Phase 3: optimize / compile (returning type responsibilities to earlier stages)**

10. [x] [ID: P1-SPEC-CONFORM2-S10] `optimize/passes/typed_repeat_materialization.py`: Removed post-hoc `resolved_type` supplementation — type supplementation for repeat/listcomp is now limited to hints (`repeat_result_type_hint`, `list_comp_result_type_hint`) rather than `resolved_type`
11. [x] [ID: P1-SPEC-CONFORM2-S11] `optimize/passes/typed_enumerate_normalization.py`: Same — `iter_expr.resolved_type` is preserved; only `iter_element_type` / `iter_item_type` / `target_type` metadata normalization is performed
12. [x] [ID: P1-SPEC-CONFORM2-S12] `compile/passes.py`: Reverted early int32 injection (restored bytes/bytearray `uint8` targets to `int64`)
13. [x] [ID: P1-SPEC-CONFORM2-S13] Regenerated golden files + confirmed parity maintained (fixture 132/132, sample 18/18, pytra 33/33)
