<a href="../../../ja/todo/archive/20260327.md">
  <img alt="日本語で読む" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

# 2026-03-27 TODO Archive

P1-CLOSURE-DEF fully completed, P2-LINK-INPUT-COMPLETENESS fully completed, P1-ISINSTANCE-NARROWING fully completed, P1-ISINSTANCE-POD fully completed, P1-PARSER-STDLIB-JSON fully completed, P1-IFEXP-OPTIONAL fully completed items archived.

---

## P1-CLOSURE-DEF: ClosureDef lowering for Nested FunctionDefs — Fully Completed

Context: [docs/ja/plans/p1-closure-def-lowering.md](../../plans/p1-closure-def-lowering.md)

1. [x] [ID: P1-CLOSURE-DEF-S1] Added the ClosureDef node specification for EAST3 to spec-east.md
2. [x] [ID: P1-CLOSURE-DEF-S2] Implemented capture analysis + ClosureDef generation in EAST3 lowering
3. [x] [ID: P1-CLOSURE-DEF-S3] Added fixtures (nested function capture patterns) + generated golden files
4. [x] [ID: P1-CLOSURE-DEF-S4] Implemented ClosureDef mapping in each emitter + confirmed parity

## P2-LINK-INPUT-COMPLETENESS: Verifying Input Completeness at the Link Layer — Fully Completed

Context: [docs/ja/plans/p2-link-input-completeness.md](../../plans/p2-link-input-completeness.md)

1. [x] [ID: P2-LINK-COMPLETE-S1] Implemented completeness verification for import resolution at the link layer (unresolved imports are reported in a fail-closed manner) — `link_modules()` now enumerates unprovided module_ids and halts
2. [x] [ID: P2-LINK-COMPLETE-S2] Defined the allowlist (verification exclusions) for runtime / stdlib modules — excluded `pytra.*` and external stdlib imports originating from runtime importers
3. [x] [ID: P2-LINK-COMPLETE-S3] Designed and implemented a type stub generation mechanism (for unparseable modules) — added `toolchain2.link.type_stubgen`, implemented EAST3 stub generation for 8 missing modules, and verified link success regression
4. [x] [ID: P2-LINK-COMPLETE-S4] Ran completeness verification against 37 selfhost modules and identified missing modules — added a regression test based on current-source regeneration rather than stored golden files

Note: Identified that the root cause of selfhost Go build failures was incomplete link input (8 missing modules). Also determined that evaluation should be based on current-source regeneration rather than stored golden files. Link now passes with stubs.

## P1-ISINSTANCE-NARROWING: Automatic Type Narrowing After isinstance — Fully Completed

Context: [docs/ja/plans/p1-isinstance-narrowing.md](../../plans/p1-isinstance-narrowing.md)

1. [x] [ID: P1-NARROW-S1] Implemented isinstance condition detection + type environment updates inside if/elif blocks in the resolver
2. [x] [ID: P1-NARROW-S2] Handled early return guards (`if not isinstance(x, T): return` followed by fallthrough narrowing)
3. [x] [ID: P1-NARROW-S3] Handled ternary isinstance (`y = x if isinstance(x, T) else None`)
4. [x] [ID: P1-NARROW-S4] Implemented narrowing invalidation upon re-assignment detection
5. [x] [ID: P1-NARROW-S5] Added fixtures (all narrowing patterns) + generated golden files + confirmed parity

Note: Narrowing is centralized in the resolver/EAST; guard-aware scope tracking in the emitter was not adopted. `JsonVal` narrows to `dict[str,JsonVal]` / `list[JsonVal]`. `and`-chained conditions and `not isinstance(...) or ...: continue` guards are included in v1.

## P1-ISINSTANCE-POD: Implementing isinstance Exact Match for POD Types + Generating golden Files — Fully Completed

Context: [docs/ja/plans/p1-isinstance-pod-exact.md](../../plans/p1-isinstance-pod-exact.md)

1. [x] [ID: P1-ISINSTANCE-POD-S1] Enabled parser / resolver to handle POD type checks like `isinstance(x, int16)`
2. [x] [ID: P1-ISINSTANCE-POD-S2] Lowered POD isinstance to exact match instructions in EAST3
3. [x] [ID: P1-ISINSTANCE-POD-S3] Generated golden files for `isinstance_pod_exact.py` (east1/east2/east3/east3-opt/linked)
4. [x] [ID: P1-ISINSTANCE-POD-S4] Verified compile + run + stdout match for C++ / Go emitters (`py_assert_stdout` passes)

Note: Since the fixture/golden files were already in place, this resolved the inconsistency between active TODOs and implementation state as of 2026-03-27. Verification included POD isinstance regressions in `test_spec_conform.py` / `test_linker_spec_conform2.py`, plus running `isinstance_pod_exact.py` with `pytra-cli.py build --target cpp --run` for C++ and `pytra-cli2.py -build --target go` followed by local `go build && ./app.out` for Go, confirming `True`.

## P1-PARSER-STDLIB-JSON: Parser Support for pytra.std.json + golden Regeneration — Fully Completed

Context: [docs/ja/plans/p1-parser-stdlib-json.md](../../plans/p1-parser-stdlib-json.md)

1. [x] [ID: P1-PARSER-JSON-S1] Enabled parser to handle `type X = ...` (PEP 695 type alias) syntax
2. [x] [ID: P1-PARSER-JSON-S2] Enabled resolution of recursive forward references inside Union types
3. [x] [ID: P1-PARSER-JSON-S3] Confirmed that `pytra.std.json.py` passes parse → resolve → compile → optimize → link
4. [x] [ID: P1-PARSER-JSON-S4] Regenerated golden files + confirmed existing parity is maintained

## P1-ISINSTANCE-NARROWING: Automatic Type Narrowing After isinstance — Fully Completed

Context: [docs/ja/plans/p1-isinstance-narrowing.md](../../plans/p1-isinstance-narrowing.md)

1. [x] [ID: P1-NARROW-S1] Implemented isinstance condition detection + type environment updates inside if/elif blocks in the resolver
2. [x] [ID: P1-NARROW-S2] Handled early return guards (`if not isinstance(x, T): return` followed by fallthrough narrowing)
3. [x] [ID: P1-NARROW-S3] Handled ternary isinstance (`y = x if isinstance(x, T) else None`)
4. [x] [ID: P1-NARROW-S4] Implemented narrowing invalidation upon re-assignment detection
5. [x] [ID: P1-NARROW-S5] Added fixtures (all narrowing patterns) + generated golden files + confirmed parity

Note: Narrowing is centralized in the resolver/EAST; guard-aware scope tracking in the emitter was not adopted. `JsonVal` narrows to `dict[str,JsonVal]` / `list[JsonVal]`. `and`-chained conditions and `not isinstance(...) or ...: continue` guards are included in v1.

## P1-IFEXP-OPTIONAL: Optional Type Inference for Ternary Expressions — Fully Completed

Context: [docs/ja/plans/p1-ifexp-optional-inference.md](../../plans/p1-ifexp-optional-inference.md)

1. [x] [ID: P1-IFEXP-OPT-S1] Updated IfExp type inference in the resolver to return `Optional[T]` for `T if cond else None`
2. [x] [ID: P1-IFEXP-OPT-S2] Made it return `UnionType` when both sides are different non-None types
3. [x] [ID: P1-IFEXP-OPT-S3] Added fixtures + generated golden files + confirmed parity

Note: Implemented `Optional[T]` / `A | B` merging for `IfExp` in the resolver. `unknown + None` is promoted to `Any | None`.
