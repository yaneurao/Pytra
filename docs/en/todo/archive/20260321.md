# 2026-03-21 Completed Tasks

## P0-CPP-GENERATED-RUNTIME-PIPELINE-01: .east → C++ Header Generation Pipeline

- Added C++ built_in/std targets to `runtime_generation_manifest.json`.
- Established `.east` → `.h` generation pipeline; fixed broken `#include` paths in `src/runtime/cpp/generated/`.

## P0-UNION-ARG-ESCAPE-01/02/03: Escape Detection for Union-Type Arguments

- Implemented parser-level heuristic to force classes used in union-type parameters to gc_managed (ref).
- Verified `Path` emits as gc_managed (inherits `RcObject`) and can be stored in `object`.
- Implemented mechanism to propagate escape analysis results into `class_storage_hint`.

## P0-ESCAPE-TO-STORAGE-HINT-01: Propagate Escape Analysis into class_storage_hint

- `optimize_linked_program` now promotes `class_storage_hint` to `"ref"` for classes appearing in union-type parameters (S1).
- Removed parser-level heuristic from `core_module_parser.py` (S2).
- Verified `Path` becomes gc_managed at the linker stage (S3).

## P0-LEGACY-API-CLEANUP-01: Purge Old object API and Legacy Compatibility Mode

- Replaced `make_object` emitter calls with `object(...)` (S1).
- Replaced `obj_to_rc_or_raise<T>` emitter calls with `object::as<T>()` (S2).
- Deleted `make_object` / `obj_to_rc_or_raise` shims from runtime (S3).
- Deleted C++ list value/pyobj mode compatibility (`cpp_list_model`); unified to ref mode (S4).
- Verified pathlib repro g++ build (S5: resolved by P0-5 link integration).

## P0-RUNTIME-EAST-IN-LINK-PIPELINE-01: Integrate runtime .east into the Link Pipeline

- Auto-include runtime module .east files referenced by `resolved_dependencies_v1` in LinkedProgram (S1).
- Include runtime modules in link emit output via `write_cpp_rendered_program` (S2).
- Retire `_generate_runtime_east_headers` (standalone transpile) (S3).
- Migrate old object API to new API (`unbox`/`as`/`is`/`_cpp_str_lit`) (S4).
- Verified pathlib repro g++ build (S5).

## P0-OBJECT-IS-TAGGED-VALUE-01: Unify object as Tagged Value

- Changed `object` definition to `{pytra_type_id tag; rc<RcObject> _rc;}` with implicit constructors and `unbox`/`as`/`is` methods (S1).
- Emitter now emits `object` for union types; retired `_Union_*` typedefs (S2).
- Changed emitter cast/isinstance/implicit-assign to `object::unbox`/`object::as`/`object::is` (S3).
- Verified g++ build including pathlib.py (S4).
- Removed compatibility layer; migrated to new API only (S5).

## P0-PY-RUNTIME-H-DECOMPOSITION-01: Decompose py_runtime.h

- Split out `core/str_methods.h` (S1).
- Split out `core/conversions.h` (S2).
- Split out `built_in/dict_ops.h` (S3).
- Split out `built_in/bounds.h` (S4).
- Split out `core/type_id_support.h`; resolved circular dependency (S5).
- Split out `core/rc_ops.h` (S6).
- Rewrote `py_runtime.h` as an include-only facade (S7).
- Changed emitter to emit individual headers; multi-file mode uses pytra_multi_prelude.h (S8).

## P0-SELF-CONTAINED-CPP-OUTPUT-01: Self-Contained out/cpp/ Build Directory

- Changed include paths for `py_runtime.h` and native headers to namespace-relative paths (S1).
- Extended `write_cpp_rendered_program` to copy native runtime into `out/cpp/{namespace}/` (S2).
- Changed emitter `#include` output paths to `out/cpp/`-relative (S3).
- Updated Makefile generation for `out/cpp/` self-containment (S4).
- Updated `pytra-cli.py --build` flow for the new pipeline (S5).
- Verified minimal repro builds with `make` inside `out/cpp/` (S6).

## P0-TAGGED-UNION-OBJECT-BOX-01: Unify Tagged Union to object + type_id

- Changed tagged union declarations to `using X = PyTaggedValue;`; added `PyBoxed`/`py_box`/`py_unbox`/`PyTaggedValue` to runtime (S1).
- Updated emitter casts (POD: `py_unbox<T, TID>(v.value)`, class: `static_cast`) (S2).
- Emitting unbox after isinstance narrowing; fixed inline union matching (S3).
- Added implicit conversion constructors to `PyTaggedValue` (S4).
- Verified g++ build including pathlib.py (S5).
- Confirmed expansion strategy for other backends (S6).

## P0-LINKER-RESOLVED-INCLUDES-01: Linker-Resolved C++ Include Paths

- Implemented `_build_resolved_dependencies` in `global_optimizer.py` (S1).
- Updated `_collect_import_cpp_includes` in `module.py` (S2).
- Fixed `compiler_headers` in `runtime_symbol_index.json` to real paths for 17 generated-only modules (S3).
- Verified minimal repro `from pytra.std.pathlib import Path` builds with g++ (S4).
- Removed `src/runtime/cpp/generated/` and C++ targets from manifest (S5).

## P1-PIPELINE-STAGE-SEPARATION-01: Pipeline Stage Separation — Decouple compile / link / emit

- Created `east2cpp.py` implementing linked EAST JSON → C++ multi-file emit (S1).
- Connected `py2x.py --link-only` output to `east2cpp.py` input (S2).
- Changed `pytra-cli.py --build` to invoke `east2cpp.py` as a subprocess (S3).
- Split `build_selfhost.py --multi-module` into a 3-stage pipeline (compile → link → emit) (S4).

## P4-VARARG-EAST3-LOWERING-01: `*args` Vararg Support — EAST3-Level Desugaring

- Added post-pass to `east2_to_east3_lowering.py`: transforms `vararg_name/type` in FunctionDef to a regular `list[T]` parameter and packs within-module call sites (S1).
- Added `_apply_vararg_callsite_packing_global` to `global_optimizer.py` for cross-module call site packing (S2).
- Added `joinpath(*parts: str | Path) -> Path` to `pytra/std/pathlib.py` (S3).

## P5-CPP-PY-IS-TYPE-DEAD-CODE-REMOVE-01: Remove py_is_type Dead Code

- Removed 7 dead functions (`py_is_dict` / `py_is_list` / `py_is_set` / `py_is_str` / `py_is_bool` / `py_is_int` / `py_is_float`) from `py_runtime.h`.
- Fixed 1 affected test. fixture/sample pass, selfhost mismatches=0.

## P5-EAST3-FLOORDIV-MOD-NODE-01: FloorDiv / Mod EAST3 IR Node

- `py_floordiv` / `py_mod` converted to EAST3 IR nodes with C++ inline emit.
- Moved `py_div` / `py_floordiv` / `py_mod` to `scalar_ops.h`.
- `py_div` inlined for arithmetic types; fallback kept for object boundary. mismatches=0. cpp 0.581.1.

## P6-CPP-LIST-MUT-IR-BYPASS-FIX-01: Fix C++ Emitter List-Mutation IR Bypass

- Routed all `py_list_*_mut()` direct-emit paths through IR nodes (ListAppend, etc.).
- 6 functions moved to `list_ops.h`; emitter updated to direct method calls (.append() etc.).
- Generated C++ files updated. mismatches=0. cpp 0.581.2.

## P6-EAST3-LEN-SLICE-NODE-01: py_len / py_slice EAST3 IR Nodes

- `py_len` moved to `base_ops.h`; emitter generates `.size()` inline.
- `py_slice` str variant renamed to `py_str_slice` in same file; list variants removed.
- `truthy_len_expr` override generates `.empty()` check. selfhost mismatches=0. cpp 0.581.3.
