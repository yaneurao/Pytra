<a href="../../../ja/todo/archive/20260329.md">
  <img alt="日本語で読む" src="https://img.shields.io/badge/docs-日本語-2563EB?style=flat-square">
</a>

# 2026-03-29 TODO Archive

P0-CPP-INCLUDE-PATH-FIX, P0-GO-PATHLIB-FIX, P1-INCLUDE-RUNTIME-MIGRATION, and P0-FAST-PARITY-CPP-BUILD completed items archived.

---

### P0-CPP-INCLUDE-PATH-FIX: Fix runtime Include Path Inconsistency in C++ emitter

1. [x] [ID: P0-CPP-INCPATH-S1] Generated `built_in/*.h` files reference `runtime/cpp/built_in/*.h`, while the main `.cpp` files reference `core/py_runtime.h`, causing inconsistent include roots. Unified include paths across all generated files. (Completed: changed native_include paths in runtime_bundle.py to be relative to `_RUNTIME_CPP_ROOT`, and added relative paths like `../built_in/contains.h` to py_runtime.h)
2. [x] [ID: P0-CPP-INCPATH-S2] Verified that `set_wrapper_methods` and `dict_wrapper_methods` compile and run successfully with g++. (Completed: both cases PASS in runtime_parity_check.py)

### P0-GO-PATHLIB-FIX: Fix Broken pathlib stdlib Signatures in Go emitter

1. [x] [ID: P0-GO-PATHLIB-S1] Fixed the Go emitter to correctly map signatures for `pathlib.joinpath` / `pathlib.read_text` / `pathlib.write_text`
2. [x] [ID: P0-GO-PATHLIB-S2] Verified that the `pathlib_extended` fixture compiles and runs successfully in Go

### P1-INCLUDE-RUNTIME-MIGRATION: Rewriting Legacy Decorators in include/ to @runtime / runtime_var

Context: [docs/ja/spec/spec-runtime-decorator.md](../../spec/spec-runtime-decorator.md)

1. [x] [ID: P1-INCLUDE-S1] Rewrote `include/py/pytra/built_in/builtins.py` using `@runtime` notation
2. [x] [ID: P1-INCLUDE-S2] Rewrote `include/py/pytra/std/pathlib.py` using `@runtime` notation (retired `@extern_fn` / `@extern_class`)
3. [x] [ID: P1-INCLUDE-S3] Rewrote `include/py/pytra/std/math.py` using `@runtime` + `runtime_var` notation
4. [x] [ID: P1-INCLUDE-S4] Rewrote `include/py/pytra/std/sys.py`, `os.py`, `os_path.py`, `glob.py`, `time.py`, `subprocess.py`
5. [x] [ID: P1-INCLUDE-S5] Removed definitions of `extern_fn`, `extern_class`, `extern_var` from `include/py/pytra/std/__init__.py`, leaving only `runtime`, `runtime_var`, and `extern`
6. [x] [ID: P1-INCLUDE-S6] Regenerated include EAST1 and verified that the resolver handles the new notation correctly (all EAST1 regenerated; key fixtures PASS)
7. [x] [ID: P1-INCLUDE-S7] Verified no impact on fixture C++/Go parity (Go pass=127 fail=19 unchanged; sys_extended has been failing from run since before and is unchanged)

### P0-FAST-PARITY-CPP-BUILD: Fix C++ Build Path in Fast parity Check

Context: [docs/ja/spec/spec-tools-parity.md](../../spec/spec-tools-parity.md) §2

1. [x] [ID: P0-FAST-PARITY-S1] `_emit_helper_cpp` in `runtime_parity_check_fast.py` was constructing the path with `module_id.split(".")`, placing output at `pytra/built_in/type_id_table.h`. Fixed to use `runtime_rel_tail_for_module`, so output is now correctly placed at `built_in/type_id_table.h`
2. [x] [ID: P0-FAST-PARITY-S2] Confirmed that the `-I emit_dir` include path in `_run_cpp_emit_dir` is consistent with the emit directory structure after the S1 fix, requiring no further change
3. [x] [ID: P0-FAST-PARITY-S3] `runtime_parity_check_fast.py --category oop --targets cpp` yields 17/18 PASS. `staticmethod_basic` fails identically to the non-fast version due to a pre-existing C++ reserved word collision with `double`
4. [x] [ID: P0-FAST-PARITY-S4] `runtime_parity_check_fast.py --targets cpp` over all 146 fixtures gives pass=115 fail=31, which is a perfect match with the non-fast version (`runtime_parity_check.py`)
