# TODO Completion History — 2026-03-31

## P0-CPP-GIF-TRANSPILE: Making pytra.utils.gif a Transpile Target

`src/runtime/cpp/generated/utils/gif.cpp` has been removed (deleted along with `generated/` during the toolchain2 migration). Because `pytra.utils.` is listed in `skip_modules` in `mapping.json`, `pytra.utils.gif` is not transpiled, resulting in a link error for undefined `py_save_gif`. This is the same structure as the TS PNG fix (removing `pytra.utils.png` from `skip_modules`).

1. [x] [ID: P0-CPP-GIF-S1] Fixed `src/runtime/cpp/mapping.json` `skip_modules` — removed `pytra.utils.gif` and `pytra.utils.png` from the skip list to make them transpile targets. Added any OS glue needed for `pyopen` / `PyFile` in the C++ runtime if necessary.
   - Completed: Confirmed that `src/runtime/cpp/mapping.json` already has `pytra.utils.*` removed from `skip_modules`, so `pytra.utils.gif` / `pytra.utils.png` are included in C++ emit targets. `pytra-cli.py build sample/py/05_mandelbrot_zoom.py --target cpp` also generates `emit/utils/gif.{h,cpp}`, and no additional `PyFile` glue was needed.
2. [x] [ID: P0-CPP-GIF-S2] Verified that GIF output sample cases (05–16) achieve compile + run parity PASS in C++.
   - Completed: `python3 tools/check/runtime_parity_check.py --targets cpp --case-root sample --east3-opt-level 2 --cpp-codegen-opt 3 --summary-json work/tmp/p0_cpp_gif_parity_summary.json 05_mandelbrot_zoom 06_julia_parameter_sweep 07_game_of_life_loop 08_langtons_ant 09_fire_simulation 10_plasma_effect 11_lissajous_particles 12_sort_visualizer 13_maze_generation_steps 14_raymarching_light_cycle 15_wave_interference_loop 16_glass_sculpture_chaos` gives 12/12 PASS, including artifact CRC32 match.

## P0-CPP-TYPE-MAPPING: Migrating C++ emitter Type Mappings to mapping.json

Spec: [spec-runtime-mapping.md](../../spec/spec-runtime-mapping.md) §7

1. [x] [ID: P0-CPP-TYPEMAP-S1] Added a `types` table to `src/runtime/cpp/mapping.json` — defined all mappings for POD types (`int64` → `int64_t`, etc.) and class types (`Exception` → `std::runtime_error`, etc.).
   - Completed: Added all 23 entries for POD types, class types, and alias types.
2. [x] [ID: P0-CPP-TYPEMAP-S2] Added a `resolve_type()` method to the `CodeEmitter` base class — a common API for resolving type names from the `types` table.
   - Completed: Confirmed existing implementation at code_emitter.py:340 `resolve_type()`.
3. [x] [ID: P0-CPP-TYPEMAP-S3] Replaced hardcoded type names in the C++ emitter (including `types.py`) with `resolve_type()` calls.
   - Completed: Added `init_types_mapping()` to types.py; emitter.py now injects mapping.types. `cpp_type()` now preferentially references the `types` table from mapping.json.
4. [x] [ID: P0-CPP-TYPEMAP-S4] Confirmed no impact on fixture parity.
   - Completed: control/for_range, stdlib/math_extended are OK.

## P3-COMMON-RENDERER-CPP: C++ emitter CommonRenderer Migration + fixture parity

Context: [docs/ja/plans/p2-lowering-profile-common-renderer.md](../../plans/p2-lowering-profile-common-renderer.md)
Spec: [docs/ja/spec/spec-language-profile.md](../../spec/spec-language-profile.md) §8

1. [x] [ID: P3-CR-CPP-S1] Migrated C++ emitter to CommonRenderer + override structure — following the profile in `src/toolchain2/emit/profiles/cpp.json`, using CommonRenderer's common node traversal. Only C++-specific nodes (ClassDef, FunctionDef, ForCore, With, etc.) remain as overrides.
   - Completed: Changed emit_stmt_extension to direct dispatch, unified `_emit_body`/`_emit_stmt` through renderer, deleted `_emit_common_stmt_if_supported`, deleted dead code (`_emit_if`/`_emit_while`). parity: for_range/if_else/exception/dataclass OK.
2. [x] [ID: P3-CR-CPP-S2] Passed C++ compile + run parity for all 132 fixtures + 18 samples — including all categories: collections 20/20, imports 7/7, stdlib 16/16.
   - Completed: commit 6e4ff3b1c — fixed header shadowing, fixed sys.h Object<list<str>> type, added emitter :: qualification, fixed JsonValue IsNot None, added str.isalnum/str.index, fixed float precision.
3. [x] [ID: P3-CR-CPP-S3] Fixed code outside include guards in C++ runtime dict_ops.h — the `py_at` implementation for tuples was placed after `#endif`, causing redefinition errors on multiple includes.
   - Completed: commit 51447a04f — moved `py_at` for tuple inside `#endif`.
4. [x] [ID: P3-CR-CPP-S4] Fixed exception safety in C++ runtime py_types.h — two-stage `new` for `PyBoxedValue` and `ControlBlock` made exception-safe. While bad_alloc is unrecoverable in practice, exception safety is guaranteed as a quality standard for transpiler-generated code.
   - Completed: commits 678317b7e / 51447a04f — migrated to `make_unique`.
5. [x] [ID: P3-CR-CPP-S5] CommonRenderer reproduces original source line breaks by referencing `source_span.lineno` in list/dict/tuple output.
   - Completed: Implemented in a previous session.
6. [x] [ID: P3-CR-CPP-S6] Unified isinstance to `pytra_isinstance` — migrated isinstance checks for primitive types (`str`, `list`, `dict`, `set`, etc.) from `py_runtime_object_isinstance` + `PYTRA_TID_*` in `type_id_support.h` to linker-generated `pytra_isinstance`. Removed legacy isinstance functions from `type_id_support.h` (`py_tid_is_subtype`, `py_tid_isinstance`, `py_runtime_value_type_id`). Note: `g_type_table` is used for destructor dispatch and is outside this task's scope. `g_type_table` removal is handled separately in P10-CPP-TYPETABLE-REDESIGN.
   - Completed: Added all primitive leaf types (None/bool/int/float/str/list/dict/set) to `_BUILTIN_CLASS_IDS` in type_id.py. Linker auto-rewrites isinstance to pytra_isinstance. Also fixed a pre-existing emitter.py missing throw semicolon bug.
7. [x] [ID: P3-CR-CPP-S7] Unified type-specific RC wrapping functions like `rc_list_from_value` / `rc_dict_from_value` into a generic `rc_from_value<T>` — the emitter only needs to wrap non-POD values with `rc_from_value(...)`, eliminating per-type function name branching in the emitter.
   - Completed: Added `rc_from_value` overloads to py_types.h; unified `_wrap_container_value_expr` in emitter.py to use `rc_from_value`. parity: for_range/comprehension/argparse_extended OK.
8. [x] [ID: P3-CR-CPP-S8] Added `_safe_cpp_ident` to the C++ emitter — appends trailing `_` to function names, method names, and variable names that collide with C++ reserved words (`double`, `class`, `int`, `float`, `namespace`, `template`, etc.), equivalent to Go emitter's `_safe_go_ident`.

## P0-EAST3-NARROWING-CAST: Inserting Cast/Unbox Nodes After isinstance Narrowing in EAST3

Context: [docs/ja/plans/p0-east3-narrowing-cast.md](../../plans/p0-east3-narrowing-cast.md)

EAST3 updates `resolved_type` after isinstance narrowing but does not insert explicit Cast/Unbox nodes. For languages like Rust that require downcasting from PyAny to a concrete type, the emitter had to handle narrowing detection itself, which violates §1.1.

1. [x] [ID: P0-EAST3-NARROW-S1] Inserted Cast/Unbox nodes on Name references where `resolved_type` changes due to isinstance narrowing — only when the original and narrowed types differ.
   - Completed: Added `UnaryOp(Not)` / `BoolOp(And)` / `IfExp` patterns in `apply_guard_narrowing` in `passes.py` and inserted Unbox nodes. `test_isinstance_narrowing.py` 6/6 PASS.
2. [x] [ID: P0-EAST3-NARROW-S2] Removed workaround from Rust emitter's `_emit_name`; now simply renders the Unbox node.
   - Completed: No workaround in `_emit_name` of `emitter.py`. Unbox nodes are processed in the `kind == "Unbox"` branch of `_emit_expr`.
3. [x] [ID: P0-EAST3-NARROW-S3] Verified no impact on fixture parity across all languages.
   - Completed: Regenerated all 131 fixtures. typing 23/23, stdlib 16/16, sample 18/18 PASS (smoke test 22/65 shows only the 43 known failures for fixtures without generated files).

## P0-RS-NARROWED-BINOP: BinOp on Narrowed union Types Falling Through to todo!()

Review note: `emitter.py:411` falls through to `todo!()` when the operand storage type is `Box<dyn Any>`. `a + b` after isinstance narrowing in EAST3 is valid, but only the Rust backend panics at runtime. The `Scalar = int | float` int branch in the `type_alias_pep695` fixture is affected.

1. [x] [ID: P0-RS-NARROWED-BINOP-S1] Used the narrowed `resolved_type` from EAST3 to emit the operation with the correct type — rather than `Box<dyn Any>` fallback, uses the concrete type after narrowing.
   - Completed: With Unbox nodes now introduced (P0-EAST3-NARROWING-CAST), BinOp child nodes are emitted with concrete types. Added inference fallback from result type / counterpart operand type to the `Box<dyn Any>` downcast logic in `_emit_binop`.
2. [x] [ID: P0-RS-NARROWED-BINOP-S2] Verified that the `type_alias_pep695` fixture achieves compile + run parity PASS in Rust.
   - Completed: Added PyStringify implementation to PyAny; confirmed runtime_parity_check_fast PASS.

## P0-RS-TYPE-MAPPING: Migrating Rust emitter Type Mappings to mapping.json

Spec: [spec-runtime-mapping.md](../../spec/spec-runtime-mapping.md) §7

1. [x] [ID: P0-RS-TYPEMAP-S1] Added a `types` table to `src/runtime/rs/mapping.json` — defined all mappings for POD types (`int64` → `i64`, etc.) and class types (`Exception` → `Box<dyn std::error::Error>`, etc.).
   - Completed: Added a types table with 37 entries to mapping.json.
2. [x] [ID: P0-RS-TYPEMAP-S2] Replaced hardcoded type names in the Rust emitter (including `types.py`) with `resolve_type()` calls.
   - Completed: Changed `rs_type()` in types.py to preferentially reference the `types` table in mapping.json. Activated with `set_mapping_types()` at emit start. `_FALLBACK_TYPE_MAP` remains for fallback.
3. [x] [ID: P0-RS-TYPEMAP-S3] Confirmed no impact on fixture emit.
   - Completed: 129/131 fixtures emit successfully (remaining 2 are pre-existing module_prefix undefined issues, unrelated to type mapping). type_alias_pep695 parity PASS.

## P0-RS-LINT-FIX: Fixing Hardcode Violations in the Rust emitter

Spec: [spec-emitter-guide.md](../../spec/spec-emitter-guide.md) §1, §7

Violations detected by `check_emitter_hardcode_lint.py`:
- module_name 2 cases: `"math": "math_native.rs"`, `"time": "time_native.rs"` — hardcoded native file names; should be derived from the runtime manifest
- runtime_symbol 3 cases: `mapped == "py_len"`, `mapped == "py_print"` — re-matching string values already resolved via mapping.json
- class_name 1 case: `"Exception": "Box<dyn std::error::Error>"` — to be resolved in P0-RS-TYPE-MAPPING

1. [x] [ID: P0-RS-LINT-S1] Fixed module_name violations — derived native file name table in `emitter.py` from runtime manifest or mapping.json.
   - Completed: Already fixed (0 violations confirmed in lint run).
2. [x] [ID: P0-RS-LINT-S2] Fixed runtime_symbol violations — removed string matches for `py_len` / `py_print`; use mapping.json resolution results directly.
   - Completed: Already fixed (0 violations confirmed in lint run).
3. [x] [ID: P0-RS-LINT-S3] Verified 0 Rust violations in `check_emitter_hardcode_lint.py`.
   - Completed: Fixed 1 class_name violation (`"Path"` hardcode) via `rs_type()`. All 7 categories PASS.

## P0-CLI2-CPP-DECOUPLE: Separating C++-Specific Imports from pytra-cli2.py

Context: [docs/ja/plans/plan-cli2-cpp-decouple.md](../../plans/plan-cli2-cpp-decouple.md)

1. [x] [ID: P0-CLI2-DECOUPLE-S1] Moved `_emit_cpp_linked_module()` and runtime_bundle import to the `toolchain2.emit.cpp` side
2. [x] [ID: P0-CLI2-DECOUPLE-S2] Changed `-emit`/`-build` C++ path in `pytra-cli2.py` to subprocess delegation
3. [x] [ID: P0-CLI2-DECOUPLE-S3] Confirmed no regression in C++ fixture + sample parity

## P0-CPP-LITERAL-CAST: Removing Redundant Casts on Integer Literals

Context: [docs/ja/plans/plan-cpp-literal-cast.md](../../plans/plan-cpp-literal-cast.md)

1. [x] [ID: P0-CPP-LITERAL-S1] Implemented `literal_nowrap_ranges` table loading + `render_constant` extension in CommonRenderer
2. [x] [ID: P0-CPP-LITERAL-S2] Set `literal_nowrap_ranges` in C++ profile/mapping and delegated integer cast logic from `_emit_constant` to CommonRenderer
3. [x] [ID: P0-CPP-LITERAL-S3] Confirmed no impact on fixture + sample parity

## P5-CPP-PARENS: Adding Operator Precedence Table to C++ emitter

1. [x] [ID: P5-CPP-PARENS-S1] Defined C++ operator precedence table and passed it to CommonRenderer
2. [x] [ID: P5-CPP-PARENS-S2] Confirmed no impact on C++ fixture + sample parity

## P6-CPP-FIXPAR: Resolving 5 fixture parity Failures

Context: [docs/ja/plans/p6-cpp-fixture-parity-failures.md](../../plans/p6-cpp-fixture-parity-failures.md)

1. [x] [ID: P6-CPP-FIXPAR-S1] Resolved `any_none` output mismatch
2. [x] [ID: P6-CPP-FIXPAR-S2] Resolved `integer_promotion` output mismatch
3. [x] [ID: P6-CPP-FIXPAR-S3] Fixed closure reference resolution in `nested_closure_def`
4. [x] [ID: P6-CPP-FIXPAR-S4] Resolved `py_zip` / `py_sum` redefinition in `ok_generator_tuple_target`
5. [x] [ID: P6-CPP-FIXPAR-S5] Resolved const qualifier inconsistency in `ok_typed_varargs_representative`

## P1-CPP-LINT-CLEANUP: Resolving emitter Hardcode Lint Violations

Context: [docs/ja/plans/plan-cpp-lint-cleanup.md](../../plans/plan-cpp-lint-cleanup.md)

1. [x] [ID: P1-CPP-LINT-S1] Resolved 3 class_name violations
2. [x] [ID: P1-CPP-LINT-S2] Resolved 1 runtime_symbol violation
3. [x] [ID: P1-CPP-LINT-S3] Resolved 1 type_id violation
4. [x] [ID: P1-CPP-LINT-S4] Resolved 9 skip_pure_python violations
5. [x] [ID: P1-CPP-LINT-S5] Confirmed no regression in fixture + sample parity

## P10-CPP-TYPETABLE-REDESIGN: Redesigning g_type_table and Destructor Dispatch

Context: [docs/ja/plans/p10-cpp-typetable-redesign.md](../../plans/p10-cpp-typetable-redesign.md)

1. [x] [ID: P10-CPP-TYPETABLE-S1] Audited all usages of `g_type_table` other than destructor dispatch
2. [x] [ID: P10-CPP-TYPETABLE-S2] Designed a solution for destructor dispatch without `g_type_table`
3. [x] [ID: P10-CPP-TYPETABLE-S3] Removed `g_type_table` and `py_tid_register_known_class_type`
4. [x] [ID: P10-CPP-TYPETABLE-S4] Confirmed no impact on fixture + sample parity

## P0-CPP-TUPLE-UNPACK: Passing tuple_unpack_variants fixture in C++
1. [x] P0-CPP-TUPLE-UNPACK-S1 — Fixed via parser/lowering/backend coordination; PASS

## P0-CPP-TYPED-CONTAINER: Passing typed_container_access fixture in C++
1. [x] P0-CPP-TYPED-S1 — Added list.extend mapping; PASS

## P0-CPP-IN-MEMBERSHIP: Passing in_membership_iterable fixture in C++
1. [x] P0-CPP-IN-MEMBER-S1/S2 — Arithmetic expansion for `range` in checks; PASS

## P0-CPP-CALLABLE: Passing callable Type in C++
1. [x] P0-CPP-CALLABLE-S1/S2 — std::function conversion, added py_lower/py_upper; PASS

## P0-CLI2-RS-DECOUPLE: Separating Rust emit-Specific Imports from pytra-cli2.py
1. [x] P0-CLI2-RS-DECOUPLE-S1/S2/S3 — Subprocess delegation; resolved Rust inflow from C++ selfhost header

## P0-RS-CALLABLE: Passing callable Type in Rust
1. [x] P0-RS-CALLABLE-S1/S2 — fn pointer conversion; PASS

## P0-RS-IN-ITERABLE: Handling in Operator with Generic iterable contains in Rust
1. [x] P0-RS-IN-ITER-S1–S5 — slice.contains(), arithmetic range check, deleted PyContains; all cases PASS

## P0-EAST3-IN-EXPAND: in Literal Expansion via EAST3 optimizer
1. [x] P0-IN-EXPAND-S1–S3 — Added optimizer pass; Rust all cases PASS; deleted tuple PyContains

## P0-EAST3-INHERIT: ref Consistency + super() Resolution for Inherited Classes
1. [x] P0-EAST3-INHERIT-S1/S2/S4 — Base ref promotion, super() resolution; inheritance_virtual_dispatch PASS

## P7-RS-EMITTER: New Rust emitter Implementation in toolchain2
1. [x] P7-RS-EMITTER-S1–S5 — emitter implementation, mapping.json; fixture 131/131 + sample 18/18 PASS

## P8-RS-LINT: Resolving Remaining Rust emitter Hardcode Lint Issues
1. [x] P8-RS-LINT-S1/S2 — class_name / skip_pure_python 0 violations

## P0-EAST-TUPLE-UNPACK: EAST tuple unpack Bug Fix (infra)
1. [x] P0-TUPLE-UNPACK-S1–S4 — Parser parenthesized LHS; comprehension expansion; C++/Rust parity PASS

## P0-PARITY-CHANGELOG: Automatic parity Change Log Recording (infra)
1. [x] P0-CHANGELOG-S1–S3 — Implemented `_append_parity_changelog`; verified operation

## P1-LINT-CHANGELOG: Automatic emitter Lint Change Log Recording (infra)
1. [x] P1-LINT-CHANGELOG-S1/S2 — Added changelog append to check_emitter_hardcode_lint.py; verified operation

## P1-CS-EMITTER: New C# emitter Implementation in toolchain2
1. [x] P1-CS-EMITTER-S1–S6 — emitter implementation, mapping.json; fixture 131/131 + sample 18/18 + stdlib 16/16 PASS; added dotnet fallback

## P2-CS-LINT-FIX: Fixing Hardcode Violations in C# emitter
1. [x] P2-CS-LINT-S1 — All categories 0 violations

## P1-JAVA-EMITTER: New Java emitter Implementation in toolchain2
1. [x] P1-JAVA-EMITTER-S1–S6 — emitter implementation, mapping.json; fixture 131/131 + sample 18/18 + stdlib 16/16 PASS

## P2-JAVA-LINT-FIX: Fixing Hardcode Violations in Java emitter
1. [x] P2-JAVA-LINT-S1 — All categories 0 violations (as of 2026-03-31; before skip_pure_python was added)
