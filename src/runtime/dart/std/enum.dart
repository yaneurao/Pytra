// enum.dart — Pytra Dart enum base classes
// source: src/runtime/dart/std/enum.dart

/// Base class for Python-style Enum (value-based identity comparison).
class Enum_ {
  const Enum_();
}

/// Base class for Python-style IntEnum (integer-compatible enum members).
class IntEnum {
  const IntEnum();
}

/// Base class for Python-style IntFlag (bitwise-combinable integer enum).
class IntFlag {
  const IntFlag();
}
