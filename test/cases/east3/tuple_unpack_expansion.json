{
  "description": "lower expands tuple unpack through a temporary and subscript assignments",
  "pipeline": "source_to_east3",
  "input": "def f() -> None:\n    (x, y) = (1, 2)\n",
  "assertions": [
    {"path": "body[0].body[0].target.id", "equals": "__tuple_unpack_0"},
    {"path": "body[0].body[1].value.kind", "equals": "Subscript"},
    {"path": "body[0].body[2].value.slice.value", "equals": 1}
  ]
}
